# Port for the QR page + API (must match OutreachOptions.gateway_url on the Python side)
PORT=8667
# Where the WhatsApp session is stored (scan the QR once, then it persists here)
AUTH_DIR=./auth
# pino log level: warn | info | debug | trace | silent
LOG_LEVEL=warn
